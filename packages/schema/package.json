{
  "name": "@pagoda/schema",
  "version": "1.0.0",
  "description": "Canonical JSON page schema for the Pagoda static website renderer",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "examples"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
